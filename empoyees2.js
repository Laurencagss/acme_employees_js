function findEmployeeByName(name,arr){
    for (let i = 0; i < arr.length;i++){
        let id = arr.length[i];
    return name + id; 
}
}



function findManagerFor(name,arr,arr2){
    for (let i = 0; i < arr.length;i++){
        let id = arr.length[i];
 for (let j = 0; j < arr2.length; j++){
     let managerId = arr2.length[i];
 }
    }
  }

